const domManipulator = (() => {
  function renderSidebar(weather) {

  }

  function renderMain(weather) {

  }
  return {
    renderSidebar,
    renderMain,
  };
})();

export default domManipulator;
